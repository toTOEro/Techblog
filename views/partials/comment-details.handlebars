<section class="card my-2 mx-3">
    <div class="card-body">
        <p class="card-text">{{comment}}</p>

    </div>

    <div class="d-flex card-header border-top ">
        <p class="ms-auto"> - <i>{{user.username}}</i> on: {{format_date comment_date}} </p>
    </div>

</section>